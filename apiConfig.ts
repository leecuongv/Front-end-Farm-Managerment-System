// This is the central configuration file for your API.
// Change the URL below to point to your backend server.

/**
 * The base URL for all API requests.
 * 
 * For local development with a backend running on port 8080, it might look like:
 * 'http://localhost:8080/api/v1'
 * 
 * For a deployed backend, it might look like:
 * 'https://api.your-farm-app.com/api/v1'
 */
export const API_BASE_URL = 'http://localhost:8080/api/v1';
